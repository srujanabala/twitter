<h1>User Profile</h1>
<%= @user.name %></br>
<%= @user.email %></br>
<%= @user.handle %></br>

<%= image_tag(@user.avatar_url) %>

<% @user_tweets.each do |tweet| %>
<div><%= tweet.content%> <%= link_to 'Show', tweet_path(tweet) %></div>
<% end %>